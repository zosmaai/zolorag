# Allow these packages to run build scripts (postinstall)
# pnpm v11+ requires explicit approval
only-built-dependencies[]=esbuild
only-built-dependencies[]=sharp
only-built-dependencies[]=@tauri-apps/cli
